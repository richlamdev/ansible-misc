---
- name: Display selinux status from gather_facts
  hosts: all
  become: True
  become_user: root
  become_method: sudo
  gather_facts: True

  tasks:
  - debug:
      msg: "{{ ansible_selinux }}"
